export class egg{
    constructor( id, eggtype, digimon,)
       {
        this.id = id,
        this.eggtype = eggtype,
        this.digimon = digimon
       }
}

export class baby{
    constructor( id, name, stage, type, digivolution,image)
       {
        this.id = id,
        this.name = name,
        this.stage = stage,
        this.type = type,
        this.digivolution = digivolution,
        this.image = image
       }
}

export class intraining{
    constructor( id, name, stage, type, preDigivolution, digivolution,image)
       {
        this.id = id,
        this.name = name,
        this.stage = stage,
        this.type = type,
        this.type = preDigivolution,
        this.digivolution = digivolution,
        this.image = image
       }
}

export class champion{
    constructor( id, name, stage, type, preDigivolution, digivolution,image, statReq, specialReq,bonus, accessArea)
       {
        this.id = id,
        this.name = name,
        this.stage = stage,
        this.type = type,
        this.type = preDigivolution,
        this.digivolution = digivolution,
        this.image = image,
        this.statReq = statReq,
        this.specialReq = specialReq,
        this.bonus = bonus,
        this.bonus = accessArea
       }
}

export class champion{
    constructor( id, name, stage, type, preDigivolution, digivolution,image, statReq, specialReq,bonus, accessArea)
       {
        this.id = id,
        this.name = name,
        this.stage = stage,
        this.type = type,
        this.type = preDigivolution,
        this.digivolution = digivolution,
        this.image = image,
        this.statReq = statReq,
        this.specialReq = specialReq,
        this.bonus = bonus,
        this.bonus = accessArea
       }
}

